<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Quern Android Driver
  ~ Forked from openatx/android-uiautomator-server (MIT License)
  ~ Copyright (c) 2015 xiaocong@gmail.com, 2017 codeskyblue@gmail.com
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- AdbKeyboard IME — the reason this APK exists -->
        <service
            android:name=".AdbKeyboard"
            android:exported="true"
            android:label="@string/adb_keyboard_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
